name: alaa_admin
description: "A new Flutter project."

publish_to: "none"

version: 1.0.0+1

environment:
  # sdk: '>=3.2.0-42.2.beta <4.0.0'
  sdk: ">=3.1.3 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http:
  auto_size_text:
  video_player:
  file_picker:
  video_thumbnail:
  path_provider:
  path:
  flex_color_scheme:
  cached_network_image:
  image_picker:
  animated_bottom_navigation_bar:
  font_awesome_flutter:
  provider:
  intl:
  syncfusion_flutter_pdfviewer: ^23.2.5
  cupertino_icons: ^1.0.2
  awesome_dialog: ^3.2.0
  pie_chart: ^5.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/

  fonts:
    - family: Kdam
      fonts:
        - asset: assets/kdam.ttf
